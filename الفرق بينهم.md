# تقرير مقارنة بين مشروع الأمن الأول والثاني

هذا التقرير يوضح الاختلافات الجوهرية بين المشروع الأصلي `security_project_1` والمشروع الجديد `security_project_2` لضمان التميز والاختلاف الكامل كما هو مطلوب.

## 1. واجهة المستخدم (User Interface)

*   **المشروع الأول:** يعتمد غالباً على تصميم تقليدي (بسيط ومباشر) قد يكون كلاسيكياً في توزيع العناصر.
*   **المشروع الثاني:** يعتمد على صيحة **Glassmorphism** الحديثة. تم استخدام خلفيات داكنة متدرجة (Radial Gradients) مع تأثيرات زجاجية ضبابية (Blur) وحدود مضيئة، مما يعطيه مظهراً احترافياً "Premium" يليق بمشاريع التخرج أو المشاريع الجامعية الكبيرة. كما تمت إضافة حركات (Animations) سلاسة عند فتح النوافذ والتبديل بين التسجيل والدخول.

## 2. هيكلة الكود (Code Architecture)

*   **المشروع الأول:** يستخدم مكتبة `Sequelize` كـ ORM للتعامل مع قاعدة البيانات، مما يجعل الكود يعتمد على الفئات (Classes) والموديلات الجاهزة.
*   **المشروع الثاني:** تم بناؤه باستخدام **Direct SQL Queries** عبر مكتبة `mysql2/promise`. هذا الاختلاف مقصود لإظهار "طريقة وأسلوب مختلف تماماً" في البرمجة، حيث يظهر الكود تحكماً أكبر في جمل الاستعلام ويكون أكثر وضوحاً في الجانب الأكاديمي لمادة الأمن.

## 3. التعامل مع التشفير (Cryptography Implementation)

*   **المشروع الأول:** ينفذ الـ RSA والـ Bcrypt بطريقة نمطية.
*   **المشروع الثاني:**
    *   تم فصل شيفرات التشفير في ملف مستقل `security.js` ليكون الكود منظماً وقابلاً لإعادة الاستخدام.
    *   تم استخدام **RSA-OAEP Padding** وهو إطار حماية أكثر تقدماً لمنع بعض أنواع الهجمات على النصوص المشفرة.
    *   الواجهة الأمامية في المشروع الثاني تقوم بعملية التشفير قبل إرسال البيانات، مع إظهار الـ Ciphertext (النص المشفر) بشكل مباشر للمستخدم لتعزيز الفهم التعليمي لعملية التشفير.

## 4. تجربة المستخدم (User Experience)

*   **المشروع الثاني** يتميز بـ:
    *   نظام تنبيهات (Toasts) عصرى بدلاً من الـ `alert` التقليدية.
    *   نافذة منبثقة (Modal) مخصصة لعرض المفتاح الخاص (Private Key) عند التسجيل.
    *   واجهة مستخدم متجاوبة (Responsive) تعمل بشكل ممتاز على الهواتف والأجهزة اللوحية.

## 5. الإعدادات والتشغيل

*   المشروع الثاني يعمل افتراضياً على بورت **3000** (بناءً على التعديل المطلوب)، وتم تغيير مسمى قواعد البيانات لتجنب أي تضارب بين المشروعين.

باختصار، المشروع الثاني ليس مجرد نسخة ثانية، بل هو إعادة بناء كاملة بأسلوب هندسي وتصميمي مختلف كلياً يجمع بين الجمالية وقوة التشفير.
